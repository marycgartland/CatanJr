package board;

public class Marketplace {
	
	// Variables
	// Constructor
	// Keep track so there is only 1 swap with the marketplace per turn
	// If all 5 places are the same resource, then we need to reset the marketplace - need a check and a reset method
	// method to let the user trade

}
